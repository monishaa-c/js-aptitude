var a= prompt("Enter a target movie name:");
var b= prompt("Enter a movie name which is running in new cinema:");
var c= prompt("Enter a movie name which is running in krishnalaya:");
if(b==a)
{
	alert("Go to new cinema");
}
else if(c==a)
{
	alert("Go to krishnalaya");
}
else
{
	alert("No way");
}